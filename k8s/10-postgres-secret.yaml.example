# 실제 값은 k8s/10-postgres-secret.yaml에 넣고 커밋하지 않는다(.gitignore에 이미 등록됨)
apiVersion: v1
kind: Secret
metadata:
  name: omok-postgres-secret
  namespace: omok
type: Opaque
stringData:
  POSTGRES_USER: change-me
  POSTGRES_PASSWORD: change-me
  POSTGRES_DB: omok
