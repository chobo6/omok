apiVersion: v1
kind: Service
metadata:
  name: omok-postgres
  namespace: omok
spec:
  clusterIP: None   # headless — 대표 IP 없이, 파드 이름으로 직접 DNS 조회되게 함
  selector:
    app: omok-postgres
  ports:
    - port: 5432
      targetPort: 5432
