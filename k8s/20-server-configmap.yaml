apiVersion: v1
kind: ConfigMap
metadata:
  name: omok-server-config
  namespace: omok
data:
  NODE_ENV: "production"
  PORT: "4000"
  CLIENT_ORIGIN: "http://localhost:4000"   # port-forward로 접속할 주소와 일치해야 쿠키/CORS가 맞음
  PGSSL: "false"                            # 인클러스터 Postgres는 SSL 없이 접속
